plugin: aws_ec2
regions:
  - us-east-1
filters:
  instance-state-name: running
  tag:OS: '*'

keyed_groups:
  - key: tags.OS
    prefix: os

hostnames:
  - private-ip-address

compose:
  ansible_host: private_ip_address